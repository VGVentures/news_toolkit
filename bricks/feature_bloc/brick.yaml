name: feature_bloc
description: Create a new feature with BLoC pattern (bloc, view, widgets)
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev <0.1.0"

vars:
  feature_name:
    type: string
    description: The name of the feature (e.g., login, feed, profile)
    prompt: What is the feature name?
