# {{storage_name.pascalCase()}} Storage

Storage abstraction for {{storage_name.lowerCase()}} data.

## Usage

```dart
final storage = {{storage_name.pascalCase()}}Storage(
  storage: persistentStorage,
);
```
